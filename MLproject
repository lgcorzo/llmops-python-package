# https://mlflow.org/docs/latest/projects.html

name: autogen_team
python_env: python_env.yaml
entry_points:
  main:
    parameters:
      conf_file: path
    command: "PYTHONPATH=src python -m autogen_team {conf_file}"
