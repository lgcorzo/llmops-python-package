---
type: class
title: "Settings"
source_path: "src/autogen_team/settings.py"
description: "Base class for application settings.  Use settings to provide high-level preferences. i.e., to separate settings from provider (e.g., CLI)."
tags: [class]
last_verified_commit: "dc137c3"
---

# Settings

Source File: `src/autogen_team/settings.py`

Base class for application settings.  Use settings to provide high-level preferences. i.e., to separate settings from provider (e.g., CLI).

## Architecture Visualization

```mermaid
classDiagram
    class Settings {
    }
```
