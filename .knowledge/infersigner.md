---
type: class
title: "InferSigner"
source_path: "src/autogen_team/infrastructure/utils/signers.py"
description: "Generate model signatures from inputs/outputs data."
tags: [class]
last_verified_commit: "dc137c3"
---

# InferSigner

Source File: `src/autogen_team/infrastructure/utils/signers.py`

Generate model signatures from inputs/outputs data.

## Architecture Visualization

```mermaid
classDiagram
    class InferSigner {
        +sign(inputs, outputs)
    }
```
